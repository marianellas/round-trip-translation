def add_mul(a, b):
    """
    Simple example function: if a > b return a*b + 1, else return a + b
    Supports integers and floats.
    """
    if a > b:
        return a * b + 1
    else:
        return a + b
